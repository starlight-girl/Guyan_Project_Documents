import {cosine,type SemanticProvider} from './SemanticProvider';
export class LocalEmbeddingProvider implements SemanticProvider{private pipeline?:any;async embed(text:string):Promise<number[]>{if(!this.pipeline){const dynamicImport=new Function('m','return import(m)') as (m:string)=>Promise<any>;const t=await dynamicImport('@huggingface/transformers').catch(()=>null);if(!t)throw new Error('可选本地模型未安装');this.pipeline=await t.pipeline('feature-extraction','Xenova/paraphrase-multilingual-MiniLM-L12-v2')}const out=await this.pipeline(text,{pooling:'mean',normalize:true});return Array.from(out.data as ArrayLike<number>)}similarity=cosine}
